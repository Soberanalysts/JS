function f(x) {
    return x + 1;
  }
  

  const f = function (x) {
    return x + 1;
  };
  

  const f = (x) => {
    return x + 1;
  };

  const f = (x) => x + 1;
  
  console.log('f(2)=', f(2)); // 3
  console.log('f(3)=', f(3)); // 4